<%= form_with(model: stat) do |form| %>
  <% if stat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stat.errors.count, "error") %> prohibited this stat from being saved:</h2>

      <ul>
        <% stat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :highscore, style: "display: block" %>
    <%= form.number_field :highscore %>
  </div>
  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id, id: :stat_user_id, value:current_user.id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
